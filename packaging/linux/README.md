This directory is intentionally lightweight.

Linux package metadata is generated by `scripts/package_linux.sh` so the CI can
package the Flutter Linux bundle into `.deb`, `.rpm`, and AppImage artifacts.

For Arch Linux (AUR) packaging details, see `packaging/aur/README.md`.
